// The "Addons" entry in the game menu.
//
// The game rebuilds #options-menu with innerHTML on every view change, so the
// entry is re-added by a MutationObserver rather than inserted once.

export function mountEscInject(): never {
  throw new Error('not implemented: game-menu injection');
}
