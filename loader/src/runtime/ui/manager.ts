// The Addons manager window: installed list, per-addon settings, keybind editor,
// logs, and diagnostics.

export function mountManager(): never {
  throw new Error('not implemented: addons manager');
}
