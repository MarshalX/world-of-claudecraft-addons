{
  "id": "wayline",
  "name": "Wayline",
  "version": "1.0.2",
  "apiVersion": 1,
  "apiMinor": 4,
  "author": "MarshalX",
  "description": "How far the next level is in time rather than in numbers: experience per hour over a rolling window, the kills and the time left to reach it, and a derived virtual level past the cap.",
  "entry": "main.js",
  "preview": {
    "file": "preview.png",
    "alt": "On the left, Levelling, a level bar over a rate, kills left and time left. On the right, Past the cap, the same panel at the cap, with a derived virtual level added."
  },
  "homepage": "https://github.com/MarshalX/world-of-claudecraft-addons",
  "tags": ["progression", "interface"],
  "permissions": ["net.read", "world.read", "ui", "storage", "keys"],
  "keybinds": [
    {
      "id": "toggle",
      "label": "Show or hide the panel",
      "default": "Alt+KeyX"
    }
  ],
  "settings": [
    {
      "id": "window-minutes",
      "type": "number",
      "label": "Minutes of play the rate is measured over",
      "default": 10,
      "min": 1,
      "max": 60
    },
    {
      "id": "rested-apart",
      "type": "boolean",
      "label": "Leave the rested bonus out of the rate",
      "default": false
    },
    {
      "id": "show-virtual",
      "type": "boolean",
      "label": "Show the virtual level past the cap",
      "default": true
    },
    {
      "id": "density",
      "type": "select",
      "label": "How tightly the panel is drawn",
      "default": "compact",
      "options": ["compact", "comfortable"]
    }
  ]
}
